Frontend scaffold for hi-ide

Run locally:
```shell
# 1. Move to frontend directory:
cd frontend
# 2. Install Node.js dependencies using npm (recommended):
npm i
# 3. Start frontend (development):
npm run dev
```